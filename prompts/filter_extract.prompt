历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题完成三件事：

【第一件】判断是否需要检索育儿知识库（need_retrieval）：
- 需要检索（true）：
  · 用户提出了育儿相关的问题，哪怕很短（例如「孩子挑食怎么办」「3-6岁呢」）
  · 用户承接上文补充了新的信息（例如「那6-12岁呢」「还有其他方法吗」）
- 不需要检索（false）：
  · 纯寒暄客套（例如「你好」「谢谢」「在吗」「你能做什么」）
  · 没有任何实际信息的内容（例如「1」「abc」「测试」「？？？」）
  · 与育儿完全无关的话题
  重要：判断依据是「用户这句话本身是否携带了有效信息」，
  不能仅仅因为它在承接上文就判定为需要检索。

【第二件】提取本次问答适用的检索条件（filters）：
- content_type：内容类型偏好，可从「育儿建议 / 专家建议 / 亲子案例 / 沟通话术 / 知识科普」中选一个，不确定则留空
- age_range：孩子年龄段，只能从「0-3岁 / 3-6岁 / 6-12岁 / 12+岁」中选择，不确定则留空
- problem_type：问题类型，例如「情绪管理 / 行为引导 / 学习能力 / 社交能力 / 亲子沟通」，不确定则留空
- scene：场景关键词，例如「发脾气 / 挑食 / 入睡困难 / 入园分离焦虑 / 作业拖延 / 手机使用」，不确定则留空

【第三件】改写用户问题（rewritten_query），使其成为不依赖上下文即可看懂的独立完整问题：
- 只有当用户使用了代词或省略（例如「这个」「它」「那6-12岁呢」）时，才结合历史会话补全；
- 补全时只能使用历史会话中明确出现过的信息，严禁添加历史里没有的场景、原因、情绪或细节；
- 如果用户的问题本身已经完整（例如「孩子挑食怎么办」），原样保留即可，不要扩写；
- 如果 need_retrieval 为 false，rewritten_query 直接返回用户原话。

【要求】
1. 任何一项无法确定时，对应字段返回空字符串，不要编造。
2. 只输出 JSON，不要输出任何解释或 Markdown 代码块。

请返回如下 JSON：
{{
  "need_retrieval": true,
  "filters": {{
    "content_type": "",
    "age_range": "",
    "problem_type": "",
    "scene": ""
  }},
  "rewritten_query": ""
}}

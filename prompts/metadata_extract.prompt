请从下面的育儿文档内容中抽取结构化元数据。

文件名：{file_title}

文档开头内容：
{context}

请抽取以下字段：
1. content_type：内容类型，只能从「育儿建议 / 专家建议 / 亲子案例 / 沟通话术 / 知识科普」中选择一个
2. age_range：适用年龄段，只能从「0-3岁 / 3-6岁 / 6-12岁 / 12+岁」中选择，可多选，多个之间用英文逗号分隔
3. problem_type：问题类型，例如「情绪管理 / 行为引导 / 学习能力 / 社交能力 / 亲子沟通 / 睡眠习惯 / 屏幕时间 / 入园适应」，可多选，多个之间用英文逗号分隔
4. scene：适用场景关键词，用一句话概括，例如「被拒绝后大哭、摔东西、躺地、尖叫」

要求：
1. 只依据文档内容判断，无法判断的字段返回空字符串，不要编造。
2. 只输出 JSON，不要输出任何解释或 Markdown 代码块。

请返回如下 JSON：
{{
  "content_type": "育儿建议",
  "age_range": "3-6岁,6-12岁",
  "problem_type": "情绪管理,行为引导",
  "scene": "被拒绝后大哭,摔东西,躺地,尖叫"
}}
